# Enable URL rewriting
# RewriteEngine On

# # Remove .php extension from URLs
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteCond %{REQUEST_FILENAME}.php -f
# RewriteRule ^(.*)$ $1.php [L]

# # Redirect .php URLs to non-extension versions (for SEO)
# RewriteCond %{THE_REQUEST} \s(.+?)\.php[\s?/] [NC]
# RewriteRule ^ %1 [R=301,L]

# # Redirect any request with the .php extension to clean URLs
# RewriteCond %{REQUEST_URI} ^/([a-zA-Z0-9_-]+)/([a-zA-Z0-9_-]+)\.php$ [NC]
# RewriteRule ^ %1/%2 [R=301,L]



RewriteEngine On

# If URL starts with qr-code/
RewriteRule ^qr-code/(.*)$ qr-code.php?qrText=$1 [L,QSA]
